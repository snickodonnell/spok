{
  "$schema": "https://schema.tauri.app/config/2/capability",
  "identifier": "default",
  "description": "Least-privilege Spok desktop capabilities. Process spawn stays on the Next local API, not the webview shell plugin. Folder pick and notifications use custom commands / notification plugin only.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "core:event:allow-listen",
    "core:event:allow-emit",
    "shell:allow-open",
    "notification:default"
  ]
}
