{
  "$schema": "https://schema.tauri.app/config/2/capability",
  "identifier": "default",
  "description": "Least-privilege Spok desktop capabilities. Process spawn stays on the Next local API, not the webview shell plugin.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "shell:allow-open"
  ]
}
