"use client";

/**
 * Mobile live watch — thin alias over shared host session sync.
 */
export { useHostSessionSync as useMobileLiveWatch } from "./use-host-session-sync";
export type { HostSessionSync as MobileLiveWatch } from "./use-host-session-sync";
